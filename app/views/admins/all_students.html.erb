<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="styles.css">
  <title>Admin Dashboard</title>
</head>
<body id="page-top">
    <header class="masthead bg-secondary text-white text-center">
            <div class="container d-flex align-items-center flex-column">

                <!-- Masthead Heading-->
                <h1 class="masthead-heading text-uppercase mb-0"><p></p></h1>
                <p></p>
                <p></p>
                <p></p>
                <p></p>
                <p></p>
                <p></p>
                <p></p>
                <p></p>
                <p></p>
                <div class="container">
  <div class="container">
    <h2>Admin Dashboard</h2>

    <h3>All Students</h3>
    <div class="search-container">
  <%= form_tag(admins_all_students_path, method: :get) do %>
    <%= text_field_tag :search, params[:search], id: 'search' , placeholder:'Enter a Name'%>
    <%= submit_tag 'Search', class: 'btn btn-primary' %>
  <% end %>
  </div>
    <table class="all-students-table">
      <thead>
        <tr>
          <th>LSN</th>
          <th>Name</th>
          <th>Age</th>
          <th>Address</th>
          <th>Emergency Contact Name</th>
          <th>Emergency Contact Number</th>
          <th>Birthdate</th>
          <th>Year</th>
          <th>Course</th>
          <th>Contact Number</th>
          <th>Edit</th> 
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
      <% @all_students.order(:name).each do |student| %>
          <tr>
            <td><%= student.usn %></td>
            <td><%= student.name %></td>
            <td><%= student.age %></td>
            <td><%= student.address %></td>
            <td><%= student.emergency_contact_name %></td>
            <td><%= student.emergency_contact_number %></td>
            <td><%= student.birthdate %></td>
            <td><%= student.year %></td>
            <td><%= student.course %></td>
            <td><%= student.contact_number %></td>
            <td>
            <%= link_to 'Edit', edit_student_path(student), class: 'btn btn-warning' %>
            </td>
            <td>
            <%= link_to 'Delete', delete_student_path(student), method: :delete, data: { confirm: 'Are you sure you want to delete this student?' }, class: 'btn btn-danger' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <p></p>
    <%= link_to 'Back', admins_dashboard_path, class: 'btn btn-warning' %>
  </div>
</body>
</html>
